# Core API framework
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1

# Data processing
duckdb==0.10.0
# OR: sqlite (built-in to Python)

# FIT file parsing (optional - GDPR import works without it)
# fitparse==1.2.0  # Has build issues on some systems, commented out for CI
# Alternative: Use python-fitparse or process FIT files separately

# Data analysis
pandas==2.2.0
numpy==1.26.4
scipy==1.12.0

# For building standalone binary
pyinstaller==6.4.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.24.0  # For async FastAPI tests (pytest 8.0+ compatible)
httpx==0.26.0           # FastAPI TestClient dependency
pytest-cov==4.1.0       # Coverage reporting
